<%= form_with(model: comment, local: true) do |form| %>
  <% if comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :comment_body %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :comment_user_id %>
  </div>

  <div class="field">
    <%= form.label :resource_id %>
    <%= form.number_field :resource_id, id: :comment_resource_id %>
  </div>

  <div class="field">
    <%= form.label :up %>
    <%= form.number_field :up, id: :comment_up %>
  </div>

  <div class="field">
    <%= form.label :down %>
    <%= form.number_field :down, id: :comment_down %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
